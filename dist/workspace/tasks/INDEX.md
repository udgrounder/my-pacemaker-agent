# 태스크 인덱스

| 태스크명 | 상태 | 요약 | 생성일 |
|---------|------|------|--------|

---

**상태값(생명주기):** `active` (진행 중) / `done` (완료) / `hold` (보류)
> 세부 진행 단계(`설계 완료` / `구현 중` 등)는 각 태스크의 `plan.md` 상태에 둔다 — INDEX와 섞지 않는다.
