---
태스크: agents-arg-input-ux
생성일: 2026-06-11
타입: minor
실패비용: minor
상태: 완료 승인
승인해시: 13864c5e9de01fac
---

# 작업 계획서: agents-arg-input-ux

**파생 출처:** 설치 작업 회고 — `install.py`를 `--agents claude codex antigravity`(공백)로 실행 시 argparse가 `unrecognized arguments`로 실패. 문서에 결합 형식이 없어 발생.

---

## 에이전트 보고

### 사용자 결정 필요

- 없음

### 암묵적 결정

- 2번(파싱) 처리 방식 — "에러 메시지 힌트 출력" 대신 `nargs='+'`(공백 구분 수용)로 결정. 사용자 승인 받음(2026-06-11). 공백/콤마 모두 동작해 에러 경로 자체가 사라지므로 더 견고함.

### 에이전트 가정

| 가정 | 근거 | 틀렸다면 |
|-----|------|---------|
| install.py/install.md는 dist/ 동기화 대상이 아님 | `ls dist/install.*` 결과 없음 확인 | dist에도 반영 필요 |

### minor 판단 근거

- 단일 관심사: `--agents` 인자 입력 UX 한 가지
- 설계 결정 불필요: nargs 변경·문서 문구 추가로 방법 자명
- git reset으로 복구 가능: 외부 상태·부작용 없음
- 사용자 취향·의사결정 불필요: 외부 연동·보안 무관

---

## 요청 원문

설치 작업 개선 사항 1·2번 처리:
1. `--agents` 다중 값 형식이 install.md에 없음
2. argparse 에러 메시지가 원인(콤마 구분)을 안내하지 못함

---

## 목적

`--agents`에 여러 agent를 넘길 때 공백·콤마 구분 모두 허용하고, 문서에 결합 형식을 명시한다.

---

## 요구사항

- `install.py`: `--agents`가 공백 구분 다중 토큰을 받아도 실패하지 않는다.
- 콤마 구분(`claude,codex`)도 기존대로 동작한다.
- `install.md`: 파라메터 표·실행 예시에 결합 형식을 명시한다.

---

## 구현 단계

- [x] Step 1 — install.py `--agents`를 `nargs='+'`로 변경, 받은 토큰 리스트를 콤마로도 재분리하도록 파싱 정규화 / 이유: argparse가 공백 토큰을 positional로 오인해 죽던 경로 제거
- [x] Step 2 — install.md 파라메터 표·실행 예시에 콤마/공백 결합 형식 명시 / 이유: 1번 문서 누락 해결 (Q2 대화형 안내는 콤마 전용이라 미수정)

---

## 수정 대상 파일

| 파일 경로 | 변경 내용 |
|---------|---------|
| `install.py` | `--agents` nargs='+' + 콤마/공백 통합 파싱 |
| `install.md` | 결합 형식 명시 (표·안내·예시) |

## 참고 파일 (수정 없음)

- `install.py` `AGENT_CONFIG_MAP` — 유효성 검사 로직 유지 확인용
