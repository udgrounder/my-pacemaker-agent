---
태스크: critique-improvements
생성일: 2026-06-10
타입: major
실패비용: major
상태: 완료 승인
승인해시: 08dd5e23d5ee40f8
---

# 작업 계획서: 비평 기반 시스템 개선 (4개 항목 통합)

**파생 출처:** 없음 — 시스템 분석/비평 세션에서 도출된 개선 항목 통합

---

## 에이전트 보고

> **구현 전에 이 섹션을 먼저 확인하세요.**

### 사용자 결정 필요

> 모두 해소됨 (GATE 1 전 결정 완료).

- **항목 2 (톤 조정 범위)** → 차단(code_gate)은 "강제" 유지, 리마인드성(세션시작 주입·종료 리마인드)을 "강제"로 묶은 표현만 "가드레일"로 완화 (선택 A)
- **항목 3 (효용 미검증 명시)** → 넣지 않음, 철회 (선택 C)
- **항목 4 (삭제역설 점검)** → 빈도 계측 메커니즘 미도입. 대신 guidebook 14.2에 사용자 통찰("위반 탐지 우선 + 비대화 시 리팩토링" + 한계)을 설명으로 보강하는 방향으로 전환

### 암묵적 결정

- SSOT(항목 1)는 "정본 신설"이 아니라 **기존 정본(agent_rules.md:218)으로 참조 포인터를 통일 + 구버전 표현 정정**으로 처리 — 이미 정본화돼 있으므로 새 정본을 만들지 않는다. 다른 방향 원하면 알려주세요.
- guidebook 한 줄 보완(2-1 논의 산물)은 본 태스크에 함께 포함.
- dist/ 동기화는 .mpa-workspace 변경분에 한해 수행(guidebook은 dist 대상 아님).

### 에이전트 가정

| 가정 | 근거 | 틀렸다면 |
|-----|------|---------|
| `workflows/bug_fix.md:37`의 "심각도/발견가능성/가역성"은 정본 ①②③ 이행 전 잔재다 | 정본(agent_rules.md:218)이 ①②③로 바뀜, INDEX 49번에 단일소스화 기록 | 의도적 보존이면 정정 대상에서 제외 |
| README:178 "agent 강제 메커니즘"은 세션주입·리마인드까지 "강제"로 묶어 과장 | 실제 강제는 code_gate 차단뿐(14.1 준수역설이 인정) | 표현 유지가 의도면 항목2에서 제외 |

---

## 요청 원문

**① 최초 검토/비평 요청 (이 개선 작업의 기원):**
"현재 이 시스템을(my-pacemaker-agent) 분석해서 가이드 문서의 내용과 문제점은 없는지 평가하고 개선사항을 알려줘. 그리고 문서의 방식에 의해 개발한 이 프로젝트의 효용성에 대해서 평가 해줘. 그리고 잘못된 부분과 개선할점을 찾아서 논리적으로 비평해줘."

**② 비평 결과를 항목별로 검토하며 나온 처리 요청:**
- 2-1(가이드 문서 함정 비판)은 범주 오류로 정정 — guidebook은 실행 경로 밖임을 확인, 일부만 유효(실행 규칙 레이어)
- 2-2(SSOT 수정) 제안 요청, 2-3 패스
- "다른것과 통합해서 계획을 세우고 처리 하자"
- AskUserQuestion 응답: "이번 개선 작업 전부 다 포함해서 하나의 계획으로 처리" (SSOT 정본화 / 강제→가드레일 톤 / 효용 미검증 명시 / 삭제역설 점검)

**③ 검토 중 확정된 가치 결정:** 톤=차단 강제 유지 / 효용 명시=철회 / 삭제역설=설명 보강 전환 (위 "사용자 결정 필요" 참조)

---

## 목적

시스템 분석 세션에서 검증된 개선 4건을, 이미 구현된 것을 재작업하지 않으면서 정합성 있게 반영한다.

---

## 요구사항

1. **SSOT 참조 정합화** — 실패 비용 추정 참조 포인터를 정본으로 통일, 구버전 표현 정정
2. **톤 조정** — 리마인드성 절차를 "강제"로 묶은 표현 정밀화 (사용자 결정 A/B 따름)
3. **효용 미검증 명시** — guidebook 14장 (사용자 결정 A/B/C 따름)
4. **삭제역설 점검** — Layer 2 체크포인트 (사용자 결정 A/B/C 따름)
5. **guidebook 보완** — 첫머리에 "실행 경로에 로드되지 않음" 한 줄

---

## 구현 단계

- [x] Step 1 (SSOT) — `session_protocol.md`, `new_feature.md`의 참조 대상을 정본 `agent_rules.md` 실패비용 절차로 통일 (완료)
- [x] Step 2 (SSOT) — `workflows/bug_fix.md` 인라인 3축 표현 제거, new_feature와 동일 참조 패턴으로 통일 (완료)
- [x] Step 3 (톤) — README, `hooks/README.md` 리마인드성 "강제"→"가드레일", **차단(code_gate)은 "강제" 유지** (완료)
- [x] Step 4 (삭제역설) — guidebook 14.2 완화책 재구성(위반탐지 우선/기원추적/빈도신호 주의 + 리팩토링 + 보고역설 한계) (완료)
- [x] Step 5 (guidebook) — guidebook.md 첫머리 "실행 경로 미로드" 한 줄 (완료)
- [x] Step 6 (동기화) — dist/ 4개 파일 동기화 일치 확인 (완료)

> 항목 3(효용 미검증 명시)은 사용자 결정으로 철회됨 — 구현 대상 아님.

---

## 예상 조용한 결정

- 톤 조정 시 어떤 문장을 "가드레일"로 바꾸고 어떤 것을 "강제"로 둘지의 경계 → 사용자 결정 A로 경계 확정(차단=강제, 리마인드=가드레일)
- 효용 미검증 단락을 14장의 어느 위치(준수역설 뒤 / 14.3 뒤)에 둘지 → 14.3(체계 실패는 인간의 몫) 뒤 권장

---

## 수정 대상 파일

| 파일 경로 | 변경 내용 |
|---------|---------|
| .mpa-workspace/core/session_protocol.md | 실패비용 참조를 정본으로 통일 |
| .mpa-workspace/workflows/new_feature.md | 참조를 정본으로 통일 |
| .mpa-workspace/workflows/bug_fix.md | 구버전 3축 표현 정정 |
| README.md | "강제 메커니즘" 표현 정밀화 |
| .mpa-workspace/hooks/README.md | "강제하는 메커니즘" 표현 정밀화 |
| .mpa-workspace/core/agent_rules.md | (필요 시) 리마인드성 "강제" 표현 검토 |
| guidebook/guidebook.md | 14.2 삭제역설 설명 보강 + 첫머리 한 줄 |
| dist/.mpa-workspace/** | 동기화 |

## 참고 파일 (수정 없음)

- .mpa-workspace/core/agent_rules.md:218 — 실패비용 추정 정본 (참조 대상)
- .mpa-workspace/personas/mpa_system_designer.md — 변경 후 일관성 점검 규칙

---

## 반례 (이 계획이 실패할 수 있는 시나리오)

- 시나리오 1: 톤 조정이 code_gate의 실제 차단력 표현까지 약화시켜 사용자가 강제력을 과소평가 → 구현 [Step 3]에 포함: 차단(code_gate)과 리마인드를 명시적으로 구분해 차단 표현은 유지
- 시나리오 2: 삭제역설 점검 추가가 '준수 지속가능성'을 위반(절차 비대화) → 해결: 메커니즘 미도입, guidebook 14.2 설명 보강으로만 처리
- 시나리오 3: 톤 조정 시 "강제" 표현을 정밀화하면서 일부를 정본(agent_rules)과 어긋나게 바꿈 → 해결: 변경 후 mpa_system_designer 일관성 점검 + grep 재확인

---

## 검증 체크리스트

- [ ] 정상 경로: 변경 후 실패비용 참조가 모두 단일 정본을 가리킨다
- [ ] 실패 경로: 구버전 3축 표현이 더 이상 grep에 잡히지 않는다
- [ ] 엣지 케이스: dist/와 .mpa-workspace/ 내용이 일치한다

---

## 완료 시 문서 업데이트 대상

- [ ] 해당 없음 (workspace/docs 변경 없음 — 본 태스크가 곧 문서 수정)

---

## 구현 후 발견

| 항목 | 유형 | 발견 맥락 | 처리 경로 |
|------|------|-----------|-----------|
| (결과를 경험한 후 채워짐) | | | |
