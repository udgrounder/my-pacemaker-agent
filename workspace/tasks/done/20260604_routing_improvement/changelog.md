# Changelog — 라우팅 개선 (문제 1)

## 수정 파일

### `dist/.mpa-workspace/core/agent_rules.md`
- 라우팅 테이블: "이어서 해줘" 행 → 작업 재개 섹션 참조로 변경
- 라우팅 테이블: "하네스 업데이트" → "체계 업데이트"
- 라우팅 테이블: 버그 행에 "(원인 분석 우선)" 제거, 대신 버그 처리 원칙 주석 추가 (사용자=증상 보고, 에이전트=진단)
- 판단 불가 처리: 케이스 α(새 요청) / β(짧은 발화)로 분리
- 라우팅 정정 발생 시 upgrade-candidates/routing_mismatches.md 기록 규칙 추가
- 작업 재개 섹션: active 1개면 바로 재개 / active 0개 or 2개 이상이면 목록 표시 후 사용자 선택 구조로 전면 교체
- 용어: "하네스 개선 후보" → "체계 개선 후보", "하네스 첫 적용" → "체계 첫 적용"

### `dist/.mpa-workspace/inject/layer1_review.md`
- "검토 진입 전 — 검토 주체 확인" 섹션 신규 추가
  - 에이전트 자가 검토 vs 사용자 보고 구분 테이블
  - 사용자 보고 진입 시 증상 수집 처리 명시
- "버그 vs 설계 미스 판별" 섹션 신규 추가
  - 구현 ≠ 설계 → 버그 (에이전트 자가 검토: 자동 수정 / 사용자 보고: 확인 후 수정)
  - 구현 = 설계 + 결과 이상 → 설계 미스 → "설계를 다시 검토하시겠어요?" → layer1_design 전환

## 계획 이탈 없음
모든 구현이 plan.md 단계와 일치함.
