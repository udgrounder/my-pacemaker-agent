# MAP Product Issues

이 source 저장소의 수집 이슈는 `inbox/<project-ref>/`에 보관한다. 검토 내용은 먼저 사용자에게 제시한다. 사용자가 채택하면 새 task plan을 연결한 뒤, 기각하면 판단 근거를 기록한 뒤 `archived/YYYY/MM/<project-ref>/`로 즉시 이동한다. archive는 이슈의 처리 결정을 보관하는 것이며, 채택된 작업의 구현 완료를 뜻하지 않는다.

이 경로는 Runtime release나 신규 설치 골격에 포함하지 않는다.
